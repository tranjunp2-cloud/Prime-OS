import { useMemo, useState } from 'react';
import { ArrowLeft, Check, ChevronDown, Image, MessageCircle, MoreHorizontal, Paperclip, Search, Send, ShoppingBag, Smile, Store, Tag, Warehouse, X } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { cn } from '@/lib/utils';

const conversations = [
  { id: 'c1', name: 'Nguyen Minh Anh', channel: 'Facebook', mark: 'f', preview: 'Can this arrive before Friday?', time: '2m', unread: 3, type: 'chat', page: 'Prime Beauty VN' },
  { id: 'c2', name: 'Tran Gia Han', channel: 'Zalo', mark: 'Z', preview: 'I want the blue variant.', time: '8m', unread: 1, type: 'chat', page: 'Prime Beauty VN' },
  { id: 'c3', name: 'Le Bao Chau', channel: 'Shopee', mark: 'S', preview: 'Is this item still available?', time: '14m', unread: 0, type: 'chat', page: 'Shopee Flagship' },
  { id: 'c4', name: 'Phan Quoc Viet', channel: 'Facebook', mark: 'f', preview: 'How do I use this product?', time: '22m', unread: 2, type: 'comment', page: 'Prime Beauty VN' },
];

const initialMessages = [
  { id: 1, from: 'customer', text: 'Hi, I would like to buy HydraGlow Essence. Is it available now?', time: '10:42' },
  { id: 2, from: 'agent', text: 'Hi Minh Anh! Yes, it is available at our HCM Central warehouse.', time: '10:43' },
  { id: 3, from: 'customer', text: 'Great. Can this arrive before Friday?', time: '10:44' },
];

const skus = [
  { sku: 'HG-ESSENCE-30', name: 'HydraGlow Essence 30ml', price: '₫684,000', available: 128 },
  { sku: 'DBC-50', name: 'Daily Barrier Cream 50ml', price: '₫600,000', available: 76 },
  { sku: 'VIT-C-SET', name: 'Vitamin C Brightening Set', price: '₫1,240,000', available: 34 },
];

export default function PrimeInboxWorkspace() {
  const [activeId, setActiveId] = useState('c1');
  const [type, setType] = useState<'chat' | 'comment'>('chat');
  const [unreadOnly, setUnreadOnly] = useState(false);
  const [query, setQuery] = useState('');
  const [message, setMessage] = useState('');
  const [messages, setMessages] = useState(initialMessages);
  const [selectedSku, setSelectedSku] = useState(skus[0]);
  const [orderCreated, setOrderCreated] = useState(false);
  const active = conversations.find((item) => item.id === activeId) ?? conversations[0];
  const visible = useMemo(() => conversations.filter((item) => item.type === type && (!unreadOnly || item.unread > 0) && (!query || `${item.name} ${item.preview}`.toLowerCase().includes(query.toLowerCase()))), [query, type, unreadOnly]);
  const sendMessage = (text = message) => { if (!text.trim()) return; setMessages((current) => [...current, { id: Date.now(), from: 'agent', text: text.trim(), time: 'Now' }]); setMessage(''); };

  return <div className="flex h-screen min-w-[1180px] flex-col overflow-hidden bg-slate-100 text-slate-900">
    <header className="flex h-14 shrink-0 items-center gap-3 border-b border-slate-200 bg-white px-4"><Button asChild variant="ghost" size="sm"><Link to="/crm/customers"><ArrowLeft className="size-4" />Main Workspace</Link></Button><span className="h-6 w-px bg-slate-200" /><span className="grid size-8 place-items-center rounded-lg bg-indigo-600 text-white"><MessageCircle className="size-4" /></span><div><h1 className="text-sm font-semibold">Prime Inbox Workspace</h1><p className="text-xs text-slate-500">Social Desk · Live conversations</p></div><span className="ml-auto inline-flex items-center gap-1.5 text-xs font-semibold text-emerald-700"><span className="size-2 rounded-full bg-emerald-500" />WebSocket connected</span><Button size="icon" variant="ghost"><MoreHorizontal className="size-4" /></Button></header>
    <main className="grid min-h-0 flex-1 grid-cols-[300px_minmax(480px,1fr)_360px]">
      <aside className="flex min-h-0 flex-col border-r border-slate-200 bg-white"><div className="space-y-3 border-b border-slate-200 p-3"><div className="flex items-center"><h2 className="font-semibold">Conversations</h2><span className="ml-2 rounded-full bg-indigo-50 px-2 py-0.5 text-xs font-semibold text-indigo-700">6 unread</span></div><div className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search conversations..." className="h-9 pl-9" /></div><select className="h-9 w-full rounded-lg border border-slate-200 bg-white px-3 text-xs font-medium"><option>All pages</option><option>Prime Beauty VN</option><option>Shopee Flagship</option></select><div className="grid grid-cols-2 rounded-lg bg-slate-100 p-1">{(['chat', 'comment'] as const).map((item) => <button key={item} onClick={() => setType(item)} className={cn('h-8 rounded-md text-xs font-semibold capitalize', type === item ? 'bg-white text-indigo-700 shadow-sm' : 'text-slate-500')}>{item}s</button>)}</div><label className="flex items-center gap-2 text-xs font-medium text-slate-600"><input type="checkbox" checked={unreadOnly} onChange={(event) => setUnreadOnly(event.target.checked)} />Unread only</label></div><div className="min-h-0 flex-1 overflow-y-auto">{visible.map((item) => <button key={item.id} onClick={() => setActiveId(item.id)} className={cn('flex w-full gap-3 border-b border-slate-100 p-3 text-left hover:bg-slate-50', activeId === item.id && 'bg-indigo-50/70')}><span className="grid size-9 shrink-0 place-items-center rounded-full bg-slate-100 text-xs font-bold text-indigo-700">{item.mark}</span><span className="min-w-0 flex-1"><span className="flex items-center"><span className="truncate text-sm font-semibold">{item.name}</span><span className="ml-auto text-[10px] text-slate-400">{item.time}</span></span><span className="mt-0.5 block text-[10px] text-slate-500">{item.page} · {item.channel}</span><span className="mt-1 block truncate text-xs text-slate-500">{item.preview}</span></span>{item.unread ? <span className="grid size-5 place-items-center rounded-full bg-indigo-600 text-[10px] font-bold text-white">{item.unread}</span> : null}</button>)}</div></aside>

      <section className="flex min-h-0 flex-col bg-slate-50"><div className="flex h-16 shrink-0 items-center gap-3 border-b border-slate-200 bg-white px-4"><span className="grid size-10 place-items-center rounded-full bg-indigo-50 text-sm font-bold text-indigo-700">NM</span><div><h2 className="text-sm font-semibold">{active.name}</h2><p className="text-xs text-slate-500">{active.channel} · Active 1 min ago</p></div><div className="ml-auto flex gap-2"><Button size="sm" variant="outline"><Tag className="size-4" />Tag</Button><Button size="sm" variant="outline">Assign <ChevronDown className="size-3" /></Button></div></div><div className="min-h-0 flex-1 space-y-4 overflow-y-auto p-5">{messages.map((item) => <div key={item.id} className={cn('flex', item.from === 'agent' && 'justify-end')}><div className={cn('max-w-[72%] rounded-2xl px-4 py-3 text-sm shadow-sm', item.from === 'agent' ? 'rounded-br-md bg-indigo-600 text-white' : 'rounded-bl-md border border-slate-200 bg-white text-slate-800')}><p>{item.text}</p><p className={cn('mt-1 text-[10px]', item.from === 'agent' ? 'text-indigo-200' : 'text-slate-400')}>{item.time}</p></div></div>)}</div><div className="shrink-0 border-t border-slate-200 bg-white p-3"><div className="mb-2 flex gap-2">{['Product availability', 'Shipping ETA', 'Order status'].map((text) => <button key={text} onClick={() => setMessage(text === 'Product availability' ? 'This item is currently available.' : text === 'Shipping ETA' ? 'Delivery is estimated within 2–3 business days.' : 'Let me check your order status now.')} className="rounded-full border border-slate-200 px-3 py-1 text-xs font-medium text-slate-600 hover:bg-slate-50">{text}</button>)}</div><div className="flex items-end gap-2 rounded-xl border border-slate-200 p-2 focus-within:border-indigo-300"><Button size="icon" variant="ghost"><Paperclip className="size-4" /></Button><textarea value={message} onChange={(event) => setMessage(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter' && !event.shiftKey) { event.preventDefault(); sendMessage(); } }} placeholder="Reply to customer..." className="min-h-10 flex-1 resize-none border-0 bg-transparent p-2 text-sm outline-none" /><Button size="icon" variant="ghost"><Smile className="size-4" /></Button><Button size="icon" onClick={() => sendMessage()}><Send className="size-4" /></Button></div></div></section>

      <aside className="min-h-0 overflow-y-auto border-l border-slate-200 bg-white"><section className="border-b border-slate-200 p-4"><div className="flex items-center justify-between"><h2 className="font-semibold">Customer information</h2><Button size="icon" variant="ghost"><X className="size-4" /></Button></div><div className="mt-4 flex items-center gap-3"><span className="grid size-12 place-items-center rounded-full bg-indigo-50 font-bold text-indigo-700">NM</span><div><p className="text-sm font-semibold">Nguyen Minh Anh</p><p className="text-xs text-slate-500">VIP · Returning customer</p></div></div><dl className="mt-4 grid grid-cols-2 gap-3 text-xs"><div><dt className="text-slate-500">Phone</dt><dd className="mt-1 font-medium">0901 234 567</dd></div><div><dt className="text-slate-500">Orders</dt><dd className="mt-1 font-semibold tabular-nums">8 · ₫6.84M</dd></div><div className="col-span-2"><dt className="text-slate-500">Address</dt><dd className="mt-1 font-medium">District 7, Ho Chi Minh City</dd></div></dl></section><section className="p-4"><div className="flex items-center gap-2"><ShoppingBag className="size-4 text-indigo-600" /><h2 className="font-semibold">Live Order Terminal</h2></div><p className="mt-1 text-xs text-slate-500">Live SKUs and ATP from Main Workspace.</p><label className="mt-4 grid gap-1.5 text-xs font-semibold text-slate-600">Product SKU<select value={selectedSku.sku} onChange={(event) => setSelectedSku(skus.find((item) => item.sku === event.target.value) ?? skus[0])} className="h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm">{skus.map((item) => <option key={item.sku} value={item.sku}>{item.name}</option>)}</select></label><div className="mt-3 rounded-lg border border-slate-200 p-3"><p className="text-sm font-semibold">{selectedSku.name}</p><p className="mt-1 font-mono text-xs text-slate-500">{selectedSku.sku}</p><div className="mt-3 flex justify-between text-xs"><span className="font-semibold tabular-nums">{selectedSku.price}</span><span className="text-emerald-700">{selectedSku.available} ATP</span></div></div><label className="mt-3 grid gap-1.5 text-xs font-semibold text-slate-600"><span className="inline-flex items-center gap-1"><Warehouse className="size-3" />Fulfillment warehouse</span><select className="h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm"><option>HCM Central · 128 ATP</option><option>Binh Duong DC · 54 ATP</option><option>Hanoi Hub · 21 ATP</option></select></label><div className="mt-3 grid grid-cols-2 gap-3"><label className="grid gap-1.5 text-xs font-semibold text-slate-600">Quantity<Input type="number" defaultValue={1} min={1} className="h-10 tabular-nums" /></label><label className="grid gap-1.5 text-xs font-semibold text-slate-600">Discount<Input defaultValue="0%" className="h-10 tabular-nums" /></label></div><Button className="mt-4 w-full" onClick={() => setOrderCreated(true)}><ShoppingBag className="size-4" />Create Draft Order</Button>{orderCreated ? <div className="mt-3 rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-xs text-emerald-800"><p className="font-semibold"><Check className="mr-1 inline size-3" />Draft order PRM-240813-009 created</p><button onClick={() => sendMessage('I created your order. Please review and confirm this checkout link: prime.to/checkout/009')} className="mt-2 font-semibold text-indigo-700">Send checkout link →</button></div> : null}</section></aside>
    </main>
  </div>;
}
