import{Input}from '@/components/ui/input';import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from '@/components/ui/select';import type{Strategy}from '@/lib/conversation-channels-api';
export interface RoutingForm{display_name:string;strategy:Strategy;target_team_id:string;sla_minutes:number}
export function Step2RoutingConfig({value,onChange}:{value:RoutingForm;onChange:(v:RoutingForm)=>void}){return <div className="grid gap-5"><label className="space-y-2"><span className="text-sm font-semibold">Display name</span><Input value={value.display_name} onChange={e=>onChange({...value,display_name:e.target.value})}/></label><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-2"><span className="text-sm font-semibold">Routing strategy</span><Select value={value.strategy} onValueChange={v=>onChange({...value,strategy:v as Strategy})}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="ROUND_ROBIN">Round-Robin</SelectItem><SelectItem value="TEAM_QUEUE">Team Queue</SelectItem></SelectContent></Select></label><label className="space-y-2"><span className="text-sm font-semibold">Destination team</span><Select value={value.target_team_id} onValueChange={v=>onChange({...value,target_team_id:v})}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="team_cs_01">Customer Success</SelectItem><SelectItem value="team_social_cs">Social Care</SelectItem><SelectItem value="team_marketplace_cs">Marketplace Support</SelectItem></SelectContent></Select></label></div><label className="space-y-2"><span className="text-sm font-semibold">SLA breach threshold (minutes)</span><Input type="number" min={1} max={1440} value={value.sla_minutes} onChange={e=>onChange({...value,sla_minutes:Number(e.target.value)})}/></label></div>}
